language: python
python:
    - "2.7"
    - "3.3"
install:
    - pip install -r requirements
script:
    - check-manifest --ignore tox.ini,test*
    - python setup.py check -m -r -s
    - flake8 .
    - py.test test
